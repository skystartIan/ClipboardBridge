<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- PC 拖過來的檔案落在 /sdcard/Download，整個外部儲存都放行讓 LineShare 取用 -->
    <external-path name="external" path="." />
    <files-path name="files" path="." />
    <cache-path name="cache" path="." />
</paths>
